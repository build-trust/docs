---
title: "Agents"
description: "Ockam Agents are intelligent autonomous actors."
icon: "star-shooting"
mode: "wide"
---

Ockam agents can break down complex queries  into detailed plans using reasoning models. Then execute the plan, step by step, using various tools at their disposal:

- LLMs to access general knowledge and language capabilities.
- Knowledge databases to augment LLMs with private or expert data.
- Tools to query remote systems.
- Memory to keep track of conversations and support durable processes.
- Other Ockam agents to distribute tasks in order to scale with the amount of work to be done.


- 

![Agent](/images/agent.png)

<Steps>
  <Step title="Install">
    To get started, install the `ockam` command by running the following in your terminal.

    ```bash
    curl -sSfL https://install.command.ockam.io | bash && source ~/.ockam/env
    ```
  </Step>
  <Step title="Deploy your first agent">
    ```bash
    mkdir hello && cd hello && ockam
    ```
  </Step>
  <Step title="Chat with your agent">
    ```
    Welcome to Ockam 👋
    
    You are connected to your agent - 782844ce-hello-main/545107ca
    Ask it a question or give it a task.
    
    Type ':help' or ':h' to see this message again.
    Type ':quit' or ':q' to disconnect.
    
    > 
    ```
  </Step>
  <Step title="Chat with your agent">
    ```
    > Tell me a tale
    ```
  </Step>
</Steps>

```bash Generated Files
» tree

├── README.md
├── ockam.yaml
└── images
    └── main
        ├── Dockerfile
        ├── README.md
        ├── main.py
        └── requirements.txt
```

```python images/main/main.py
from ockam import Agent, Node, Repl
from sys import argv


async def main(node):
    agent = await Agent.start(node, "You are Jack Sparrow.")
    await Repl.start(agent, argv[1])


Node.start(main)
```

```yaml ockam.yaml
name: hello
pods:
  - name: main-pod
    containers:
      - name: main
        image: main
        args: [localhost:9000]
    portals:
      outlets:
        - to: localhost:9000
```

```python images/main/main.py
from ockam import Agent, Node, Repl
from sys import argv


async def main(node):
    agent = await Agent.start(node, "You are Henry, an expert legal assistant")
    await Repl.start(agent, argv[1])


Node.start(main)
```

run

```bash
ockam
```