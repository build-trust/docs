---
title: "Agents"
description: "Ockam Agents are autonomous actors."
icon: "star-shooting"
---


<Steps>
  <Step title="Install" iconType="regular" stepNumber={1}>
    ```bash
    curl -sSfL https://install.command.ockam.io | bash
    ```
  </Step>
  <Step title="Run">
    ```bash
    ockam
    ```
  </Step>
</Steps>


<RequestExample>

```python images/main/main.py
from ockam import Agent, Model, Node, Repl


async def main(node):
    agent = await Agent.start(node, "You are Jack Sparrow")
    await Repl.run(agent)


Node.run(main)
```


```yaml ockam.yaml
name: example01
pods:
  - name: main
    containers:
      - name: main
        image: main
        args: [127.0.0.1:9000]
    portals:
      outlets:
        - to: 127.0.0.1:9000
```

</RequestExample>